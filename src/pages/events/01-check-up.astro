---
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import { LinkButton } from "@astrojs/starlight/components";
---

<StarlightPage frontmatter={{ title: 'KOREC 일본취업 진단', description: '...' }}>
  <h2>📌 일정</h2>
  <h4>매월 선착순 접수! </h4>
  <h5>• 온/오프라인 가능 </h5>
  <p>오프라인: 서울, 부산 중 택 1</p>

  <br/><h2>📝 소개</h2>
  <p>신청자의 상황을 파악 후, 최적의 플랜 및 <b>Next Action</b>을 제안 드리는 서비스입니다.</p>

  <script is:inline>
    document.addEventListener('DOMContentLoaded', () => {
      const urlParams = new URLSearchParams(window.location.search);
      const queryString = urlParams.toString() ? `?${urlParams.toString()}` : '';
      document.querySelectorAll('a[icon="external"]').forEach(link => {
        if (!link.href.includes('?')) link.href += queryString;
      });
    });
  </script>

  <LinkButton href="https://cafe.naver.com/koreccafe/5264" variant="primary" icon="external" target="_blank">
    자세한 정보 보기
  </LinkButton>
</StarlightPage>