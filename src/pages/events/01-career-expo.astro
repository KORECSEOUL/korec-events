---
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import { Card, LinkButton } from "@astrojs/starlight/components";

// 쿼리 파라미터 로직 추가
const url = new URL(Astro.request.url);
const utmSource = url.searchParams.get('utm_source') || '(not set)';
const utmMedium = url.searchParams.get('utm_medium') || '(not set)';
const utmCampaign = url.searchParams.get('utm_campaign') || '(not set)';
const queryString = utmSource !== '(not set)' ? `?utm_source=${utmSource}&utm_medium=${utmMedium}&utm_campaign=${utmCampaign}` : '';
---

<StarlightPage frontmatter={{ title: 'KOREC CAREER EXPO 2025', description: '...' }}>
  <h2>📌 일정</h2>
  <h4>2025년 7월 11일(금) 11:00~ </h4>
  <h5>• 명동 온즈드롬 </h5>
  <p>서울특별시 중구 퇴계로 115 명동 밀리오레호텔 PH층</p>

  <br/><h2>📝 소개</h2>
  <p>SEGA, 사이버 에이전트 등 우량기업 10개사가 참가하는 최대 규모 채용 이벤트!</p>

  <LinkButton href={`https://cafe.naver.com/koreccafe/9475${queryString}`} variant="primary" icon="external" target="_blank">
    자세한 정보 보기
  </LinkButton>
</StarlightPage>