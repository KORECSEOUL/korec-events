---
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import { Card, LinkButton } from "@astrojs/starlight/components";

// 쿼리 파라미터 로직 추가
const url = new URL(Astro.request.url);
const utmSource = url.searchParams.get('utm_source') || '(not set)';
const utmMedium = url.searchParams.get('utm_medium') || '(not set)';
const utmCampaign = url.searchParams.get('utm_campaign') || '(not set)';
const queryString = utmSource !== '(not set)' ? `?utm_source=${utmSource}&utm_medium=${utmMedium}&utm_campaign=${utmCampaign}` : '';
---

<StarlightPage frontmatter={{ title: 'KOREC 일본취직 CLASS', description: '...' }}>
  <h2>📌 일정</h2>
  <h4>2025년 8월 1일(금) 19:00~</h4> 
    <h5>• 온라인 </h5>

<br/><h2>📝 소개</h2>
  <p>50기 역사의 일본취업스터디 전격 리뉴얼!
<br/>기존 일본취업스터디가 <b>'KOREC 일본취직 클래스🎓'</b>로 돌아왔어요
<br /><br />- 입학부터 졸업까지 전 30일 코스 
<br /><br />- 동영상 콘텐츠(전 12편) + 4회 온라인 면담 구성
<br /><br />- 1회 30분~ 1시간, 수강생이 원하는 스케줄대로 자유롭게 동영상 콘텐츠 수강 
<br /><br />- 면담 + 종업식 시 <b>KOREC 커리어 어드바이저</b>의 일본 취직 개별 어드바이스 제공 & 서포트!</p>
  <script is:inline>
   document.addEventListener('DOMContentLoaded', () => {
      const urlParams = new URLSearchParams(window.location.search);
      const queryString = urlParams.toString() ? `?${urlParams.toString()}` : '';
      document.querySelectorAll('a[icon="external"]').forEach(link => {
        if (!link.href.includes('?')) link.href += queryString; 
      });
    });
  </script>
  <LinkButton href="https://cafe.naver.com/koreccafe/9786" icon="external" target="_blank">
    신청하기
  </LinkButton>

  <style>
    .link-button {
      display: inline-block;
      margin-top: 1rem;
      padding: .75rem 1.25rem;
      background-color: var(--color-accent-600);
      color: white;
      border-radius: .5rem;
      text-decoration: none;
      font-weight: bold;
    }
    .link-button:hover {
      opacity: .9;
    }
  </style>
</StarlightPage>